<div class="course">
    <h1>{{course.title}}</h1>
    <img src="{{course.img}}" alt="{{course.title}}">
    <p>{{course.description}}</p>
    <hr>
    <span class="price big">{{course.price}}</span>

</div>
